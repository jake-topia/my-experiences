I just made a config variable called lightControlMode which can be the string "auto" or "user". Auto means the current implementation, user should route to a new light control implementation where one user is selected to control the lights. I also added a configuration variable called obstacleGenerationType which can be either "random" or "set". Set means the implementation we have now, random means a not too random implementation we've yet to make. lightControlMode set to user will also include new red areas which may be a more straightforward path to the end but during a red light a user could press a button to teleport any players in those red areas.

great, now we need to create the user-controlled version. Importantly the first round of this version needs to pick a random player to be the light leader. We need a .75s timer to delay the leader from spamming the light on and off, we need to also ensure the yellow light stays as a warning with its .75s timer. Additionally we'll add two "red zones" to the map, each zone is connected to button also in control of the leader. If the leader presses this button during a red light, after a short delay the red zone will go from its normally slightly opaque version to full opaque, teleporting the players inside the zone back. Ill make the sprites for the buttons and zones as follows:



Lets define a new thing called the game closing, when the game is closing we should kick all remaining players and that will end the experience, view the docs on how to kick players. Then, change the code such that when there's only 1 player left alive and this player is not the light leader, they win. If the final remaining player is the light leader, we should just close the game without ending it - perhaps we can check this condition inside the onPlayerLeft built-in func. Finally, if the game has restarted 8 times already, instead of restarting the 9th time, close the game.


Looks like the danger zones are staying opaque after being activated, we only want them turning opaque for about a half second and then fading back to its normal opacity value of .4 over the next second using the animation strategy in Tiptoe Ice Prod. Also, they should be teleporting players. This teleport should only happen the instant the button is pressed and for the players colliding with a danger zone. I am making a new engine variable called dangerZonePlayerMap which should keep track of all the players in the danger zones dynamically. We should use the engine builtin funcs onSpriteCollisionStart and onSpriteCollisionStop similar to how they are used in TipToe Ice Prod to keep track of which players are in danger zones and keep that list updated.

1. Need to make the buttons appear when non-host users are light leader
2. Need to make choosing system for choosing the next light leader. Make it such that we now allow 3 players to complete the RLGL round, we ensure existing winners do not get teleported after the light turns red again after winning, we make sure to delay the round end/winner announcement logic until 3 people have made it. Once 3 have made it then we end the round and announce the winner similar to how the game already does. Then we clear all the street assets as usual and teleport those 3 winners to the center (spaced out evenly), we then do some rng math to decide which winner will be the next leader. Once decided, we animate the text 'Next Light Leader v' to move between the 3 winners, slowing down over time, and stopping on the previously rng decided next light leader. Given the decision has been made and we still have enough players to play the next round, we immediately move to the next round.




The sprites you make for the players names look good, however they arent cleared or set opacity 0 when the next round actually starts - we need to do that. Next lets approach how we do the Next player decision differently, Let's stioll decide beforehand, but now lets use an animation similar to the animation example in the AI-HELP document from TipToe Ice Prod. In this animation we'll set the duration super low like at like 100, and then we'll use the onLoop: () => { call to actually animate the sprite. Define a number of positions where the pointer sprite should be (the sprite that says "Next Light Leader v"). The positions will be the places above the player text sprite locations (just get the player name text position and minus 200 px in the Y). We'll start on the left position with the pointer sprite (position 1), move to position 2 on the next loop, then position 3, then position 1, and so on. After 32 of these, we'll only move to the next position every other loop. Then after 16 of those, we'll only move to the next position every 4th loop. Then after 8 of those, we'll only move to the next position every 8th loop. Then after 4 of those we'll only move to the next position every 16th loop. Then, we'll move the remaining number of positions in the same 1,2,3,1,... pattern until we hit the desired final position and we'll do this last pattern once every 20th loop.